import { useNavigate } from '@tanstack/react-router'

const NotFount = () => {
  const navigate = useNavigate()

  const goHome = () => {
  }

  return (
    <div>404</div>
  )
}

export default NotFount
